version: '2'  
services:  
  web:
    image: testing:latest
    container_name: GRAPHSPACEXDOCKER
    command: bash -c "/GraphSpace/docker_config/docker_command.sh"
    restart: unless-stopped
    volumes:
      - /GraphSpace
    expose:
      - "8000"
    ports:
      - "8000:8000"
